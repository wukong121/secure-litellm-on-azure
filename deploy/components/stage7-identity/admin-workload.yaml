apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-admin-proxy
spec:
  replicas: 2
  minReadySeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  selector:
    matchLabels:
      app.kubernetes.io/name: entra-auth-proxy
      plane: admin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: entra-auth-proxy
        plane: admin
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: llm-admin-proxy
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 600
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: auth-proxy
          image: REPLACE_APPROVED_ACR_AUTH_PROXY_DIGEST
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PROXY_PLANE
              value: admin
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          startupProbe:
            httpGet: {path: /healthz, port: http}
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            periodSeconds: 20
          lifecycle:
            preStop:
              exec:
                command: [/bin/sh, -c, "sleep 30"]
          volumeMounts:
            - name: config
              mountPath: /etc/auth-proxy
              readOnly: true
            - name: secrets
              mountPath: /mnt/auth-secrets
              readOnly: true
      volumes:
        - name: config
          configMap: {name: llm-auth-policy}
        - name: secrets
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes: {secretProviderClass: llm-admin-auth}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: entra-auth-proxy
              plane: admin
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: llm-admin-proxy
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: entra-auth-proxy
      plane: admin