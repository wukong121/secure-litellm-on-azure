apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - workloads.yaml
  - admin-workload.yaml
  - ingress.yaml
  - secrets.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: llm-auth-policy
    files:
      - policy.json
patches:
  - path: backend-network-patch.yaml
  - path: backend-identity-patch.yaml